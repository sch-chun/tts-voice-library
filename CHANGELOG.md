# Changelog

## [1.0.1] - 2026-08-31

### 新增
- 加入音量调节滑条，可调节播放音量。

## [1.0.0] - 2026-08-31

### 重构 (Breaking Changes)
- **架构迁移**：从单一 HTML 文件重构为基于 React + Vite 的模块化单页应用，项目结构清晰，易于扩展。
- **构建工具**：采用 Vite 作为构建工具，启动和构建速度显著提升，支持 HMR。

### 新增
- **虚拟滚动**：引入 `@tanstack/react-virtual`，表格行仅渲染可视区域，大幅减少 DOM 节点，解决大数量音色（如 200+ 条）时切换主题或滚动卡顿的问题。
- **主题切换**：增加深色/浅色模式切换，通过 CSS 变量统一管理，所有组件自适应主题。
- **模块化组件**：
  - `Header`：顶栏，包含标题、副标题和主题切换按钮。
  - `FilterBar`：筛选栏，支持搜索、性别、语种、年龄范围、特质/场景多选（包含/排除模式）。
  - `VoiceTable`：表格容器，负责数据过滤、排序、虚拟滚动。
  - `VoiceRow`：单行数据展示，包含音频播放、复制 voice_id、排除按钮。
  - `StatsBar`：统计筛选结果数。
  - `Toast`：全局提示组件。
  - `ThemeContext`：主题状态管理。
- **复制功能升级**：使用现代 `navigator.clipboard` API，移除已弃用的 `document.execCommand`。

### 修复
- **深色模式兼容**：修复深色模式下表格单元格背景色错误（原为白色）、占位文字颜色过暗导致不可见的问题。
- **列对齐**：采用 Grid 布局替代原 table 布局，表头与表体列宽严格对齐，防止内容错位。
- **筛选换行**：优化搜索、性别、语种、年龄等控件布局，使其在桌面端保持同一行。
- **排除模式**：修复多选筛选「排除」模式下背景色未变红的问题。
- **播放高亮**：修复播放时切换筛选或排序后，高亮行位置错误的问题（后续迭代将进一步优化）。

### 性能
- **虚拟滚动**：仅渲染可视区行，滚动流畅度提升。
- **组件懒加载**：按需加载，首屏加载时间缩短。

### 依赖更新
- React 18.3.1
- Vite 5.0
- @tanstack/react-virtual 3.0.0-beta.54
- Bootstrap 5.3.2（仅样式）
- Font Awesome 6.0.0-beta3

---
